import React from 'react';

const PromptMessage = ({ children }) => {
    return (
        <div className="promtMessage_container">
            {children}
        </div>
    );
};

export default PromptMessage;
